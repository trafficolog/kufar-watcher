# v0.3.0 — MVP-1 acceptance checklist

> Status: **IN PROGRESS — live evidence required**
>
> Canonical task: `docs/tasks/5-0-4-mvp-acceptance.md`.
> This checklist records manual acceptance only. CI, unit tests and synthetic
> fixtures are supporting evidence and do not replace the live run.

## Acceptance build / source

- Base `main` (synchronized 2026-09-16): `43fa723be3f4d0895488fa75c791f0ae3b774bb8`
- Acceptance branch: `qa/5-0-4-mvp1-acceptance`
- Run mode: source/development (`npm run dev`); installers are out of scope for `0.3.0`
- Started: 2026-09-15
- Tester / machine: pending
- OS: pending
- Exact Windows acceptance build/commit: pending (record after checkout, not inferred from CI)

## Defect resolution preflight (not manual acceptance evidence)

- [x] #72 reversible archive implementation merged via PR #73 in `43c8b49d`; full Linux CI GREEN: https://github.com/trafficolog/kufar-watcher/actions/runs/35075829876.
- [x] #70 renderer CSP implementation merged via PR #75 in `c598c1ad`; full Linux CI GREEN: https://github.com/trafficolog/kufar-watcher/actions/runs/35084923157.
- [x] #71 supported URL validation and terminal `unsupported-api-mapping` merged via PR #74 in `43fa723b`; full Linux CI GREEN: https://github.com/trafficolog/kufar-watcher/actions/runs/35085338419.
- [ ] Verify the integrated acceptance-branch CI on the exact recorded head.
- [ ] Windows dev renderer mounts with default and occupied default ports; HMR works; no `NUXT_E1009`, `wrappedConfig` TypeError or unapproved external script/font permission.
- [ ] Windows production `app://` renderer mounts with the generated per-build inline-script CSP hashes.
- [ ] Unsupported Kufar URL is rejected in UI before persistence without a new monitor; a legacy unsupported monitor produces a terminal, non-retried failure if available.
- [ ] Archive regression on a disposable monitor: cancel leaves state unchanged; confirm preserves history, removes future schedule/notifications after restart; supported restore works; unsupported legacy restore is rejected; other monitors remain unaffected.

These implementation merges remove the previously identified **code-integration** blockers. Their manual Windows regression gates and the live MVP criteria below remain unverified. Archive retains Monitor/Run/Match/Cursor; irreversible deletion is not part of MVP-1.

## Authoritative URL shapes

### A — Minsk games / consoles

- URL: `https://www.kufar.by/l/r~minsk/igry-i-pristavki/q~ps5`
- Required CanonicalQuery semantics: host `www.kufar.by`, region `minsk`, category
  `igry-i-pristavki`, operation `null`, `pathFilters=[]`, query `ps5`
- Result: **pending**

### B — Minsk apartments for sale

- URL: `https://re.kufar.by/l/minsk/kupit/kvartiru`
- Required CanonicalQuery semantics: host `re.kufar.by`, region `minsk`, category
  `kvartiru`, operation `kupit`, `pathFilters=[]`, no search query
- Result: **pending**

Successful acceptance of these two shapes does not imply support for other
regions/categories/operations. In particular it does not cover regionless
`igry-i-pristavki`, rental apartments, `doma`, `/elektronika`, Auto, or other
unconfirmed semantics.

## 0. Clean-profile precondition

Perform before launching the acceptance session. These operations are setup, not
part of the user-flow acceptance itself. Archive/legacy regression must be run
separately from the exactly-two-monitors clean-profile observation.

- [ ] Application is stopped.
- [ ] Docker volume `kufar-watcher-postgres-data` is removed so Postgres starts
      with an empty data directory and migrations recreate an empty application DB.
- [ ] `telegram-token.enc` is absent from Electron `app.getPath('userData')`.
- [ ] Application starts successfully with an empty monitor list / first-run state.
- [ ] No existing Telegram binding or token is shown.

Evidence / notes:

- Pending.

## 1. Telegram live gate (`5.0.2`)

This section closes the two remaining live checks from `5.0.2` when run on
Windows with a real bot/chat.

- [ ] Enter a real bot token in `/settings` and validate it from the UI.
- [ ] Save the token; UI reports protected storage (not unprotected fallback).
- [ ] Windows storage path contains `telegram-token.enc`; its stored value does
      not contain the plaintext token.
- [ ] Send a message to the bot, wait for the candidate to appear, inspect the
      candidate identity, and bind only by pressing the UI action.
- [ ] Send the built-in test message.
- [ ] Test message arrives in the bound Telegram chat.
- [ ] No token value appears in the application journal/output inspected during
      this flow.

Evidence / notes:

- Pending.

## 2. Create both monitors from the UI

No database or file inspection is allowed for this section.

- [ ] Create monitor A using the exact games URL above.
- [ ] Create monitor B using the exact real-estate URL above.
- [ ] Both monitors appear in `/monitors` with their interval/state visible.
- [ ] First traversal for A completes without a burst of notifications for old listings.
- [ ] First traversal for B completes without a burst of notifications for old listings.
- [ ] Last traversal/result updates in the list without manually refreshing or inspecting DB state.

Evidence / notes:

- Monitor A name / interval: pending
- Monitor B name / interval: pending
- Cold-start notification count A: pending
- Cold-start notification count B: pending

## 3. Live finding — shape A

- [ ] Wait for a new matching listing after A's cold-start baseline.
- [ ] Telegram notification arrives in the bound chat.
- [ ] Notification content corresponds to a listing present in the games/consoles
      Minsk result set and satisfies configured local matching terms.
- [ ] `Открыть` opens the expected human Kufar listing.
- [ ] Listing was not already part of the baseline at monitor creation.

Evidence / notes:

- Listing URL: pending
- Notification time: pending
- Application Run/result time: pending
- Duplicate observed: pending
- If duplicate: corresponding uncertain-resend journal evidence: pending

## 4. Live finding — shape B

- [ ] Wait for a new listing after B's cold-start baseline.
- [ ] Telegram notification arrives in the bound chat.
- [ ] Notification corresponds to a listing present in the Minsk apartments-for-sale result set.
- [ ] `Открыть` opens the expected human Kufar listing.
- [ ] Listing was not already part of the baseline at monitor creation.

Evidence / notes:

- Listing URL: pending
- Notification time: pending
- Application Run/result time: pending
- Duplicate observed: pending
- If duplicate: corresponding uncertain-resend journal evidence: pending

## 5. Restart recovery

Perform with both monitors active.

- [ ] Quit the application normally.
- [ ] Start it again without modifying DB, files or monitor configuration.
- [ ] Both monitors are present and active after restart.
- [ ] Subsequent traversals occur for both monitors.
- [ ] A new qualifying finding after restart is not lost.
- [ ] No unexplained duplicate is produced by restart.

Evidence / notes:

- Restart time: pending
- First post-restart traversal A: pending
- First post-restart traversal B: pending
- Post-restart live finding evidence: pending

## 6. Observation window

Leave exactly these two acceptance monitors active for several hours and compare
notifications against the corresponding Kufar result pages manually.

- Observation start: pending
- Observation end: pending
- Monitor A notifications: pending
- Monitor B notifications: pending
- Missing qualifying listings found manually: pending
- Unexplained duplicates: pending
- Explained uncertain-state resends: pending

## 7. Final task criteria

- [ ] E2E passes without DB/file access for both request-buildable shapes.
- [ ] No misses observed for either monitor during the observation window.
- [ ] Any duplicate is explained by persisted uncertain-send/journal evidence.
- [ ] Restart preserves both active monitors and subsequent processing.
- [ ] Real links and separate results for both shapes are recorded above.
- [ ] Any discovered defect has a GitHub issue linked below.
- [ ] Acceptance result does not widen the declared routing perimeter.

Defects:

- #70 renderer CSP — implementation merged; Windows manual regression pending.
- #71 supported URL validation/no-retry — implementation merged; Windows manual regression pending.
- #72 reversible archive — implementation merged; Windows manual regression pending.

## Preflight notes

- On 2026-09-15 the real-estate acceptance URL returned a live Minsk apartment
  result page during external preflight.
- Generic web retrieval of the games URL was blocked with HTTP 403. This is not
  an acceptance failure: the required evidence is the production application's
  own polite client path, not a third-party web fetch.
- `5.0.2` remains `in_progress` until the Windows protected-storage and real
  Telegram test-message checks above are completed.
