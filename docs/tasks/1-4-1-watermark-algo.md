---
id: "1.4.1"
phase: 1
epic: "1.4"
status: done
sync_state: aligned
last_reviewed: 2026-09-08
roles: [BACK]
depends_on: ["1.3.1"]
estimated_hours: 4-5
agent: backend-senior
tags: [core, tdd]
---

# Задача 1.4.1 — Алгоритм водяного знака

> Эпик 1.4 · Фаза 1 · ✅ done · зависит от: 1.3.1 · оценка: 4-5 ч

## Цель

TDD. Определить, какие объявления в выдаче являются новыми, идя по выдаче вниз до временной границы предыдущего обхода.

## Контекст

Это ядро смысла продукта. Ошибка в одну сторону даёт пропущенные объявления, в другую — повторные уведомления. Логика содержит неочевидные случаи: одинаковое время размещения у нескольких объявлений, объявление, поднятое в выдаче, удалённое объявление на границе.

Отдельно про глубину. Кажется, что первой страницы достаточно: при минутном интервале новых объявлений между обходами единицы. Но первая страница переполняется каждый раз, когда обходы прерывались — перезапуск, сон машины, сбой сети, всплеск публикаций. Именно в этот момент водяной знак уходит за её пределы, и монитор теряет всё, что между ними, оставаясь внешне здоровым. Догоняющий обход из эпика 2.5 без глубины не реализуем вовсе.

## Что должно быть сделано

- Сначала тесты на синтетических выдачах: нет новых, часть новая, все новые,
  граница за пределами первой страницы, объявление с границы удалено с площадки,
  пять объявлений с одинаковым временем на границе, время границы встречается и
  выше и ниже неё
- Реализовать обход страниц по курсору до совпадения с водяным знаком: типичный случай — одна страница, но алгоритм обязан идти глубже
- Водяной знак хранить парой: время размещения `T` и множество идентификаторов,
  виденных **ровно на границе** `T`. Алгоритм строится на времени, а не на поиске
  конкретного идентификатора:
  - объявления с `listTime > T` — кандидаты в новые;
  - на `listTime == T` пройти всю границу целиком и отбросить те, чьи
    идентификаторы уже известны: несколько объявлений могут иметь одинаковое
    время, и остановка на первом совпадении обрезала бы остальные;
  - при первом `listTime < T` обход останавливается;
  - новый водяной знак — максимальное встреченное время и множество
    идентификаторов на нём.
- Исчезновение конкретного объявления при таком построении ничего не ломает: его
  идентификатор нигде не ищется
- Ограничить продвижение потолком страниц; при его исчерпании вернуть результат с признаком возможного пропуска, чтобы вызывающий код сообщил пользователю
- Учесть, что каждая дополнительная страница — запрос через лимитер: потолок задаётся конфигурацией, а не вшивается
- Вернуть список новых объявлений и новое значение водяного знака

## Критерии приёмки

- [x] Все сценарии из тестов проходят
- [x] Повторный прогон на той же выдаче возвращает пустой список новых
- [x] Граница, оказавшаяся на третьей странице, находится, и все объявления выше неё возвращаются как новые
- [x] Удаление объявления, по которому был поставлен водяной знак, не приводит ни к пропуску, ни к предупреждению, ни к обходу до потолка страниц
- [x] Пять объявлений с одинаковым временем на границе обрабатываются целиком: ранее не виденные попадают в новые
- [x] Исчерпание потолка страниц приводит к предупреждению, а не к молчаливому пропуску
- [x] Тесты написаны до реализации

## Проверка

Основная synthetic suite: `tests/unit/watermark-traversal.test.ts`.

Покрыто и проверено в Actions:

- no/some/all new и полный проход границы `T`;
- повторный прогон с возвращённым watermark даёт пустой `newListings`;
- boundary на третьей странице с передачей opaque cursor без декодирования;
- удалённая boundary-запись: остановка по `listTime < T` без регрессии watermark и без `possibleMiss`;
- несколько unseen ID на одинаковом `T` обрабатываются целиком и объединяются с известными boundary IDs;
- page cap возвращает `possibleMiss: true`, кандидатов уже прочитанных страниц и неизменённый предыдущий watermark;
- first-occurrence dedup для overlap между страницами и поднятых объявлений;
- typed ordering guard внутри страницы и между страницами, включая эквивалентные ISO offset spellings;
- ошибки `SourceAdapter` сохраняются по identity.

TDD-последовательность сохранена в истории ветки: RED-коммиты `ab8e3378` / `5394da63` / `ecfae4cb` предшествуют соответствующим production GREEN-коммитам; итоговый behavioral/style head `3eb0885894857b2df17e88f49fe0a26f2d0304bf` подтверждён полным verify run #414 (`34247013203`).

## Границы ответственности

- Персистентность watermark и транзакционная граница принадлежат задаче `1.4.2`.
- Семантика cold start принадлежит задаче `1.4.3`.
- Доставка пользовательского предупреждения для `possibleMiss` принадлежит последующей orchestration/UI-логике; `1.4.1` только возвращает явный признак.
- Marketplace cursor остаётся локальным транспортным значением одного обхода и не декодируется, не синтезируется и не персистится.

## Подсказки

- Общий контекст — в карточке эпика и в `docs/AGENTS.md`

## Не делать

- Не обращаться к базе: алгоритм чистый, водяной знак и множество известных идентификаторов на границе приходят параметром
- Не выполнять сетевых запросов напрямую: страницы поставляет адаптер, переданный параметром
