---
id: "1.1.1"
phase: 1
epic: "1.1"
status: todo
sync_state: aligned
last_reviewed: 2026-09-07
roles: [BACK]
depends_on: ["0.1.3", "1.0.2"]
estimated_hours: 4-5
agent: backend-senior
tags: [parser, tdd]
---

# Задача 1.1.1 — Разбор URL листинга в CanonicalQuery

> Эпик 1.1 · Фаза 1 · ⬜ todo · зависит от: 0.1.3, 1.0.2 · оценка: 4-5 ч

## Цель

TDD. Превратить адрес отфильтрованной выдачи kufar.by в структуру `CanonicalQuery`.

## Контекст

Единственный способ ввода — вставка готовой ссылки. Значит, разбор адреса и есть интерфейс продукта: ошибка здесь означает, что пользователь мониторит не то, что думает. Функция чистая, детерминированная и не требует сети — идеальный кандидат на тесты вперёд кода.

## Что должно быть сделано

- Описать тип `CanonicalQuery`: категория, поисковая строка, регион, тип продавца, сортировка, прочие параметры
- Сначала тесты в `tests/unit/url-parse.test.ts` на наборе реальных адресов: с поисковой строкой, с регионом, без региона, с сегментом «без посредников», с категорийными параметрами
- Реализовать разбор пути и строки запроса
- Неизвестные параметры сохранять как есть, а не отбрасывать: они уйдут в запрос без изменений
- Нераспознаваемый адрес — явная ошибка с указанием причины, не молчаливое пустое правило

## Критерии приёмки

- [ ] Все тестовые адреса разбираются корректно
- [ ] Неизвестные параметры сохраняются в структуре
- [ ] Адрес не с kufar.by отклоняется с внятной ошибкой
- [ ] Тесты написаны до реализации, что видно по истории коммитов

## Design alignment — 2026-09-07

Утверждённый дизайн зафиксирован в `docs/superpowers/specs/2026-09-07-1-1-1-canonical-query-design.md`, план реализации — в `docs/superpowers/plans/2026-09-07-1-1-1-canonical-query.md`.

Имя тестового файла нормализовано с исторического `.spec.ts` на `.test.ts`, потому что текущий `vitest.config.ts` намеренно собирает `tests/**/*.test.ts`. Глобальный test-discovery ради одной карточки не расширяем.

## Подсказки

- Отображение сегментов адреса на параметры API описано в `docs/superpowers/specs/kufar-api-contract.md`; parser сохраняет семантику пользовательского URL, а API mapping относится к задаче `1.1.2`

## Не делать

- Не выполнять сетевых запросов в этой задаче
- Не заниматься валидацией существования категории — это требует таксономии, которой в MVP нет
